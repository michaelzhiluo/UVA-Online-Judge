#include <iosteam>
#include <bits/stdc++.h>

using namespace std;

int main(){
	int numpeople;
	long int budget;
	int numhotels;
	int numweeks;
	while(true){
		scanf("%d %ld %d %d", &numpeople, &budget, &numhotels, &numweeks);
		

	}
	
}